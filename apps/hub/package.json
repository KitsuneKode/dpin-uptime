{
  "name": "@dpin-uptime/hub",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "dev": "bun run --hot src/index.ts",
    "start": "bun run dist/index.js",
    "lint": "bun run lint"
  },
  "devDependencies": {
    "@types/bun": "^1.2.23",
    "@dpin-uptime/typescript-config": "*",
    "tsdown": "^0.15.4"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@dpin-uptime/auth": "*",
    "@dpin-uptime/common": "*",
    "@solana/web3.js": "^1.98.4",
    "tweetnacl": "^1.0.3",
    "tweetnacl-util": "^0.15.1"
  }
}
